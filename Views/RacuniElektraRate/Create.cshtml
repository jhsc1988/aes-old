@model IEnumerable<aes.Models.RacunElektraRate>


@{
    ViewData["Title"] = "Create";
}
<div class="content-bg">
    <div class="head-bg">
        <p class="h6">Elektra - rate</p>
    </div>
    <div class="container">
        <div>
            <partial name="_RacuniAddNew" />
        </div>
    </div>
    <div class="space-10-10">
        <div id="ind">
            <table class="display nowrap w-100" id="indexTable">
                <thead>
                    <tr>
                        <th>Radni broj</th>
                        <th>Broj računa</th>
                        <th>Stan ID</th>
                        <th>Adresa</th>
                        <th>Korisnik</th>
                        <th>Vlasništvo</th>
                        <th>Datum izdavanja</th>
                        <th>Iznos</th>
                        <th>Napomena</th>
                        <th>Akcija</th>
                    </tr>
                </thead>
            </table>
            <br />
            <hr />
            <div class="row">
                <div class="margin-20">
                    <button type="button" class="dt-button" id="btnSave">Spremi</button>
                </div>
                <div class="margin-20">
                    <button type="button" class="dt-button" id="btnDelete">Briši sve</button>
                </div>
            </div>
        </div>
        <hr />
        <div>
            <button onclick="location.href='@Url.Action("Index", "RacuniElektraRate")'" type='button' class='button-back' id='remove'><i class='bi bi-arrow-left'></i>  povratak na listu</button>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script>
        let dopisiForFilter;
        const isItForFilter = false;
        const selectDopis = $("#selectDopis");

        //predmet filter
        const GetPredmetiDataForFilterUrl = "/RacuniElektraRate/GetPredmetiCreate";
        const GetDopisiDataForFilterUrl = "/RacuniElektraRate/GetDopisiCreate";

        $("#selectDopis").change(function () {
            data_dopis = $("#selectDopis :selected").val();
            refreshWithFilteredData();
        });

        function refreshWithFilteredData() {
            $('#IndexTable').DataTable().ajax.reload();
        }
    </script>

    <script src=~/js/_js_global/notificationBuilder/GlobalVariables.js></script>
    <script src=~/js/_js_global/notificationBuilder/NotificationBuilder.js></script>
    <script src="~/js/_js_global/filterPredmetiDopisi.js"></script>

    <script src="~/js/RacuniElektraRate/Create/CreateDatatables.js"></script>

    @* alertify *@
    <script src=~/js/alertify/alertify.min.js></script>
    <link rel=stylesheet href=~/js/alertify/css/alertify.css>

    @* search style *@
    <script src="~/js/DatatablesSearchStyle.js"></script>
    <script>
        function GetKupciData() {
            $.ajax({
                type: "POST",
                url: "/RacuniElektraRate/GetKupci",
                success: function (kupci) {
                    kup = JSON.parse(kupci);
                }
            });
        }
        $(document).ready(function () {
            const input = $('#indexTable_filter input[type="search"]');
            const filter = $('#indexTable_filter');
            setStyle(input, filter);
        });
    </script>
}
